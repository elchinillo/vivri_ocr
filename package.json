{
    "engines": {
        "node": "4.0.0"
    },
    "scripts": {
        "postinstall": "cd ui/ && npm install --only=dev && npm install",
        "heroku-postbuild": "cd ui/ && npm run build"
    }
}
